apiVersion: v1
kind: Service
metadata:
  name: ufotracker-service
  namespace: ufology
  labels:
    app: ufotracker
spec:
  type: ClusterIP
  selector:
    app: ufotracker
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080